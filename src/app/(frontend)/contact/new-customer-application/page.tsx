import Bounded from '@/components/Bounded';

const NewCustomerApplication = () => {
  return (
    <Bounded className="grid place-items-center min-h-screen">
      <div className="bg-brand-purple text-white p-5 font-bold">
        Page Under Maintainence
      </div>
    </Bounded>
  );
};

export default NewCustomerApplication;
